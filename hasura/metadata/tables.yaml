- table:
    schema: public
    name: objects
  insert_permissions:
  - role: anonymous
    permission:
      check: {}
      columns:
      - id
      - value
      backend_only: false
  select_permissions:
  - role: anonymous
    permission:
      columns:
      - id
      - value
      filter: {}
  update_permissions:
  - role: anonymous
    permission:
      columns:
      - id
      - value
      filter: {}
      check: null
